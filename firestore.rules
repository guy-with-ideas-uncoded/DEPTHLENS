rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write across all documents and subcollections for comprehensive sync & recovery
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
